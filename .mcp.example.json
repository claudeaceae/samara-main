{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--user-data-dir", "~/.claude-mind/playwright/user-data",
        "--output-dir", "~/.claude-mind/playwright/output",
        "--save-session"
      ]
    }
  }
}
